<Window x:Class="Hushward.App.Tray.TrayFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Hushward.App.Localization"
        Title="{x:Static loc:UiText.AppTitle}"
        Width="330"
        Height="284"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        Topmost="True"
        Background="{StaticResource SurfaceBrush}"
        FontFamily="{StaticResource AppFontFamily}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="{Binding StateLabel}"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <TextBlock Margin="0,6,0,0"
                       Text="{Binding PrimaryReason}"
                       Foreground="{StaticResource QuietGrayBrush}"
                       TextWrapping="Wrap" />
        </StackPanel>
        <StackPanel Grid.Row="1"
                    Margin="0,14,0,0">
            <TextBlock Text="{x:Static loc:UiText.TrayProtectionCountLabel}"
                       Style="{StaticResource CaptionTextStyle}" />
            <TextBlock Text="{Binding ProtectionCount}"
                       FontSize="18"
                       FontWeight="SemiBold" />
            <TextBlock Margin="0,10,0,0"
                       Text="{Binding IdleText}"
                       Foreground="{StaticResource QuietGrayBrush}" />
        </StackPanel>
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button HorizontalAlignment="Left"
                    Command="{Binding ToggleEnabledCommand}"
                    Content="{Binding ToggleEnabledText}" />
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal">
                <Button Command="{Binding PauseTodayCommand}"
                        Content="{x:Static loc:UiText.PauseToday}" />
            </StackPanel>
            <StackPanel Grid.Row="1"
                        Grid.ColumnSpan="2"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button Margin="0,0,8,0"
                        Command="{Binding OpenMainCommand}"
                        Content="{x:Static loc:UiText.TrayOpen}" />
                <Button Command="{Binding ExitCommand}"
                        Content="{x:Static loc:UiText.TrayExit}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
